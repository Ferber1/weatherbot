import asyncio

#* uncomment import class of bot you need
# from bots.tgbot import TgBot
# from bots.vkbot import VkBot

if __name__ == '__main__':
    """
    Please, create object of bot you need:
    
    >>> bot = TgBot()
    >>> asyncio.run(bot.start())

    or
    
    >>> bot = VkBot()
    >>> asyncio.run(bot.start())
    """
    